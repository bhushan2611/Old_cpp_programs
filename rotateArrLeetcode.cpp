#include <bits/stdc++.h>
using namespace std;
void rotate(vector<int> &nums, int k)
{
    // int n = nums.size(), count = 0;
    // int arr[n];
    // for (int i = n - k; i < n; i++)
    //     arr[count++] = nums[i];

    // for (int j = 0; j < n - k; j++)
    //     arr[count++] = nums[j];

    // for (int j = 0; j < n; j++)
    //     cout << arr[j] << " ";
}
int main()
{
    vector<int> arr{1,2,3,4,5,6,7};
    rotate(arr,3);
    return 0;
}